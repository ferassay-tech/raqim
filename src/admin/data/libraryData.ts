import type { LibraryFile } from "../types/library";

// Real production data only — no downloadable files have been uploaded
// yet, so this starts genuinely empty rather than seeded with fake demo
// files (same precedent as Milestone 3's production-shell conversion).
export const INITIAL_LIBRARY_FILES: LibraryFile[] = [];
